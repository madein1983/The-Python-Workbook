# area = (b*h)/2  where b is length ob triangle's base and h is height of triangle

b = float(input("Enter the length of triangle's base: "))
h = float(input("Enter the height of triangle: "))
area = (b*h)/2
print(area)